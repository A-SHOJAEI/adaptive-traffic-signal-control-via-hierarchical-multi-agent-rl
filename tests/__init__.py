"""Test suite for hierarchical multi-agent RL traffic control system."""

import os
import sys

# Add src to path for testing
src_path = os.path.join(os.path.dirname(__file__), "..", "src")
sys.path.insert(0, src_path)